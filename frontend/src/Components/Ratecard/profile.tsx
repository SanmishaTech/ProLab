import React from "react";

const MultiSelectorComponent: React.FC<any> = ({ values, onChange }) => {
  return (
    <div className="multi-selector">
      {/* Simple placeholder component for the profile selector */}
      <p>Multi-selector component</p>
    </div>
  );
};

export default MultiSelectorComponent; 